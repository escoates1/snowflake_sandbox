name: snowflake_sandbox
version: "1.0.0"
profile: snowflake_sandbox

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  snowflake_sandbox:
    staging:
      +schema: staging
      +materialized: table
    marts:
      +schema: marts
      +materialized: table
