select
    event_key,
    event_id,
    date_key,
    location_key,
    event_class_key,
    alert_status_key,
    event_timestamp,
    event_url,
    detail_url,
    latitude,
    longitude,
    depth_in_km,
    magnitude,
    direction_from_place,
    distance_from_place_km, 
    felt_count,
    cdi_max,
    mmi_max,
    significance,
    num_stations,
    dmin_deg,
    rms_sec,
    azimuthal_gap_deg
from {{ ref('fact_seismic_event') }}